// import { render, screen } from '@testing-library/react';
// import { Header } from '../Header';
// import { ClerkProvider } from '@clerk/nextjs';

// TODO: This component is difficult to test with Jest due to its
// dependencies on Next.js and Clerk. We should revisit this test
// when we have a more robust end-to-end testing setup with a tool
// like Cypress.
describe.skip('Header', () => {
  it('renders a heading', () => {
    // render(
    //   <ClerkProvider>
    //     <Header />
    //   </ClerkProvider>
    // );
    // const heading = screen.getByRole('heading', {
    //   name: /vibe hacker security/i,
    // });
    // expect(heading).toBeInTheDocument();
  });
});
